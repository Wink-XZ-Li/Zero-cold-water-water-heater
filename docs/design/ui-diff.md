# UI 差异记录（SC-005 / 005-ux-polish）

**日期**: 2026-08-13  
**对照**: Ardot file `714289030938546` · 方案修改 `55:241` · 首页 `55:788` / `55:711` · 故障 `55:781` · 报告 `55:1044`

## 状态

首页组件级对齐 + 报告页双卡壳。须 IDE 截图复核；未复核前不算清零。

## 本轮补齐（对照用户截图漏项）

- 开机行「关机」文字：Ray `Text` 不吃 class 色，改为行内色
- 英雄卡设置温度 160rpx，℃ 贴数字右上
- 卫浴卡：海军蓝顶栏 + 左侧白圈凹槽 + 喷头矢量（`55:832`）
- 故障横幅右箭头：浅蓝圆底
- 能耗报告：白头卡（图标+标题+用水量/用气量白芯胶囊）+ `#738BBA` 内容卡（白底日周月年、白字日期+圆箭头、内嵌白图表卡、白字±15%）
- 「使用模式」28rpx Bold

## 已对齐

- 页面浅蓝底 + 深蓝英雄卡 + 出水温度按 DP 原样
- 开机行矢量电源；开/关文字与选中色（产品例外）
- 四模式矢量、入口矢量
- 故障：警示矢量 +「故障提醒：{码} {说明}」+ 就地摘要
- 内容水平边距 `32rpx`、主块 `gap: 32rpx`

## 已接受例外

1. **NavBar**：保留 SmartUI NavBar；对齐从内容区第一块起算。
2. **电源开/关色**：Ardot 两帧电源行都是白底「开机」；实现关=白底「开机」、开=深蓝底「关机」。
3. **关机灰态**：`switch===false` 时模式/滑条/零冷水开关/瀑布浴禁用变灰；预热与能耗报告仍可进入。
4. **故障手册页**：不做；横幅点击就地摘要。
5. **火焰 / 水流 / 进水温度**：不在 005-B。
6. **出水温度脏值**：按 DP 原样显示（含虚拟器 `-27℃`）。
7. **能耗图表数据**：StatCharts 请求/`dataTransformer`/配色不改。日视图单根柱是虚拟器用量，不是壳层缺失。

## 已关闭的过时项

- ~~模式/入口单字占位~~ → 矢量
- ~~故障只显示码~~ → 码 + 说明
- ~~电源开/关无区分~~ → 文字 + 选中色
- ~~报告页单白卡~~ → `55:1044` 白头卡 + 蓝内容卡

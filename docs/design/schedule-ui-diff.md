# 002 定时 UI 对照（米家信息结构）

**日期**: 2026-08-13  
**参考**: Ardot `57:5`（列表）、`57:4`（编辑）  
**视觉**: 001 伊莱克斯 token（非米家青绿）

## SC-004 信息结构

| 米家结构 | 本面板实现 | 状态 |
|----------|------------|------|
| 列表标题「定时」类 | NavBar「定时列表」 | 对齐 |
| 行主文案 `HH:mm - HH:mm` | `ScheduleGroupItem` 主标题 | 对齐 |
| 行副文案 开启时段\|重复 | `开启时段 \| …` | 对齐 |
| 行右侧开关 | SmartUI Switch 成对 status | 对齐 |
| 右下角新增 | FAB `+` | 对齐 |
| 编辑：重复多选 | 日一二…圆钮 | 对齐 |
| 编辑：开启/关闭时间 | 行 + DatetimePicker | 对齐 |
| 编辑：执行通知默认关 | Switch 默认 false | 对齐 |
| 删除二次确认 | Dialog confirm | 对齐 |

正式伊莱克斯定时帧到位后，仅替换视觉细节（图标/间距），不改上表信息架构。
